# Backend secret configuration (example only)
# Copy to backend/.env and set real values locally/server-side.

NODE_ENV=development
PORT=8080
ANTHROPIC_API_KEY=replace_with_real_key
ALLOWED_ORIGIN=https://claude.ai
