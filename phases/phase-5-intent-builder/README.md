# Phase 5 - Intent Builder

## Goal
Normalize prompt + answers into a structured intent object.

## Deliverables
- Intent schema and validation
- Builder service combining prompt and clarifications
- Debug view of generated intent JSON in sidebar

## Acceptance Criteria
- All required intent fields are populated or explicitly null
- Intent object is stable across retries for same inputs
